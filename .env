# Copy this file to .env in the same folder as docker-compose.yml and fill in real values.
# .env should be in .gitignore — never commit real secrets.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=Mybday_28

# Must match (or replace) the jwt.secret currently in each service's application.properties
JWT_SECRET=BuzzApp2026SecretKeyForCodeQuestKNUST
